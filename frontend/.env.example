# Optional. Leave empty to talk to the same origin, which is what both the
# Vite dev proxy and the production build served by Express already do.
# Set it only when the API lives on a different host, e.g. a deployed backend.
VITE_API_URL=
