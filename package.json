{
  "name": "lost-found-item-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Lost & Found Item Tracker - PCE SW PS 13 (Express API + matching engine + React UI + optional AI service)",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm run build",
    "seed": "npm --prefix backend run db:seed",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend start",
    "start:api": "npm --prefix backend start",
    "demo": "node scripts/demo-flow.js",
    "test:ui": "node scripts/ui-smoke.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  },
  "engines": {
    "node": ">=18"
  }
}
